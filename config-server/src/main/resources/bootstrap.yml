spring:
  application:
    name: configserver
  cloud:
    config:
      server:
        git:
          uri: https://github.com/dmitry-lee/spring-cloud-config-server-repository.git
          default-label: main
          username: dmitry-lee
          password: ***REMOVED***
          clone-on-start: true
    fail-fast: true
  security:
    user:
      name: spring_cloud_user
      password: ***REMOVED***